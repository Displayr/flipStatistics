# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
warnings_are_errors: false

before_install:
  Rscript -e 'update.packages(ask = FALSE)'

after_script: >-
  curl -s -X POST -H "Content-Type: application/json" -H "Accept: application/json" -H "Travis-API-Version: 3" -H "Authorization: token $ACCESS_TOKEN" -d '{"request":{"message":"Build triggered by flipStatistics", "branch":"master", "notifications":{"slack":{"rooms":"numbers:FTgSTNHC2rpanhJMGTKMwZXM"}}}}' "https://api.travis-ci.org/repo/chschan%2FtestCI/requests"

